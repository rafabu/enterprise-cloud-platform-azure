# ECP Launchpad Bootstrap Finalizer

This module doesn't deploy anything. It is only used as a placeholder to provide summary output
and remove backend firewall exceptions once bootstrapping (the entire level 0) is finished.

## Output

- launchpad_infrastructure
  - ado_agent_pool (optional)
  - launchpad_subscription