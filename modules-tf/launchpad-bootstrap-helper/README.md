# ECP Lauchpad Bootstrap Helper

This module doesn't deploy anything. It is merely producing datasource-based output to
consume by launchpad wrapper scripts.

## Output

- backend_resource_group
- backend_storage_accounts

Note: Those aren't the actual resources but simulated ones, predicting the (future) names and ids.